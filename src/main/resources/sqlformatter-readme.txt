Simple SQL Formatter formats SQL statements. It is intended for use as
an API, from the command-line, via a built editor.

Options:
    -h
        Shows this help
    -window
        Shows the built-in editor
    <none>
        Reads stdin, and writes stdout